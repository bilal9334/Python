def greet(name: str) -> str:
    """
    Returns a greeting message including the given name.

    :param name: The name of the person to greet.
    :return: A greeting message as a string.
    """
    return f"Hello, {name}"


print(greet("Bilal"))
